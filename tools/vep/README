====================== Melbourne Genomics Variant Calling Pipeline =======================

VEP is used by the pipeline to compute CONDEL scores.

For VEP to work, you need to install its data, by running something like:

perl INSTALL.pl -c ../vep_cache 

from within the vep folder (named by version, currently 83)

